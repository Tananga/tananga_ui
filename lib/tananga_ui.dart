import 'package:tananga_ui/src/buttons/buttons.dart';

/// A Calculator.
class TanangaUI {
  /// Returns [value] plus 1.
  // int addOne(int value) => value + 1;
  static final buttons = Buttons();
}
